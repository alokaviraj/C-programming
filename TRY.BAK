#include<stdio.h>
include<conio.h>
int main()
{
int n,m;
printf("enter n:");
scanf("%d",&n);
//printf("enter m:");
//scanf("%d",&m);
if(n>0)
//if(m=0)
printf("true");
else
printf("false");
return 0;
getch();
}