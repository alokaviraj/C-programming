#include<stdio.h>
#include<conio.h>
void main()
{
int i,n,key,found=0;
int arr[10];
clrscr();
printf("enter the number of elements");
scanf("%d",&n);
printf("insert the element in the array\n");
for(i=0;i<n;i++)
scanf("%d",&arr[i]);
printf("enter the element to be searched");
scanf("%d",&key);
for(i=0;i<n;i++)
if(key==arr[i])
{
found=1;
break;
}
if(found==1)
printf("%d is preset at location %d",key,arr[i]);
else
printf(" data is not present");
getch();
}
